{
  "name": "class-order",
  "version": "0.0.1",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "concurrently 'npm:svelte:dev' 'npm:react:dev'",
    "svelte:dev": "cd examples/svelte && npm run dev",
    "react:dev": "cd examples/react && npm run dev",
    "build:examples": "concurrently 'npm:svelte:build' 'npm:react:build'",
    "svelte:build": "cd examples/svelte && npm run build",
    "react:build": "cd examples/react && npm run build",
    "preview": "concurrently 'npm:svelte:preview' 'npm:react:preview'",
    "svelte:preview": "cd examples/svelte && npm run preview",
    "react:preview": "cd examples/react && npm run preview",
    "build": "TODO"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^10.9.0",
    "bun-types": "^0.7.3",
    "concurrently": "^8.2.0",
    "tslib": "^2.4.1",
    "typescript": "^4.9.4"
  },
  "peerDependencies": {
    "@happy-dom/global-registrator": "^10.9.0"
  }
}