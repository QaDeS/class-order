export function merge(cls : string)
export function forceMerge(cls : string)
export function classOrder(el : HTMLElement, opts : {override: boolean = false})
export function forceClassOrder(el : HTMLElement)
